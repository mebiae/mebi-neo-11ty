<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>
            {% if title %}
                {{title}}
                {% if title and sub %}
                    - {{sub}}
                {% endif %}
            {% elif sub %}
                The Mebiverse Hub - {{sub}}
            {% else %}
                The Mebiverse Hub
            {% endif %}
        </title>
        <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta property="og:url" content="https://afellowu.neocities.org/">
        <meta property="og:type" content="website" />

        {% if metatitle %}
        <meta property="og:title" content="{{metatitle}}"/>
        {% else %}
        <meta property="og:title" content="{{site.title}}"/>
        {% endif %}

        {% if desc %}
        <meta property="og:description" content="{{desc}}"/>
        {% else %}
        <meta property="og:description" content="Welcome to the Inter-Universal Hub, where Mebi's universes and galaxies are connected together."/>
        {% endif %}

        <meta property="og:image" content="https://afellowu.neocities.org/images/thumbnail.png" />
        <meta property="twitter:card" content="summary_large_image">
        <link rel="stylesheet" type="text/css" href="/css/ocbase.css" />
    </head>
    <body>
        {{ content | safe }}
    </body>
</html>