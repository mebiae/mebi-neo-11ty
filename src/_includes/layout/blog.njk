---
title: MebiBlogs

layout: layout/base.njk
---

<style>
    .blogposts {
        margin: 2em 0;
    }

    .blognav {
        display: flex;
        justify-content: center;
        gap: 1em;
    }

    .blognav a {
        text-align: center;
        background-color: #141a3e;
        padding: 1em;
    }
</style>

{% set prevPost = collections.blogs | getPreviousCollectionItem %}
{% set nextPost = collections.blogs | getNextCollectionItem %}

<main>
    <article>
        <h1>{{ sub }}</h1>
        <blockquote>{{ blogdesc }}</blockquote>
        <small>Posted on {{ page.date.toLocaleDateString("en-CA") }}</small>
        <hr>
        {{ content | safe }}

        <div class="blognav">
            {% if prevPost %}<a href="{{ prevPost.filePathStem }}">&lt;-- Previous Post</a>{% endif %}
            {% if nextPost %}<a href="{{ nextPost.filePathStem }}">Next Post --&gt;</a>{% endif %}
        </div>
    </article>
</main>